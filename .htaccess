# Route all requests through index.php (optional, for clean URLs)
# Remove this file if using nginx or another web server.

Options -Indexes

# Prevent direct access to data directory
<FilesMatch "^data/">
    Require all denied
</FilesMatch>

# PHP settings
php_flag display_errors Off
php_value error_reporting 0
